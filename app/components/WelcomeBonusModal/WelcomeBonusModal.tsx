import { type FC } from 'react';

/**
 * Welcome bonus popup was removed per product requirements.
 * We keep the component stubbed to avoid breaking imports elsewhere.
 */
export const WelcomeBonusModal: FC = () => null;

export default WelcomeBonusModal;

